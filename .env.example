# Optional. When set, room codes + history persist across restarts.
# On Render: use the Internal Database URL of a Render Postgres instance.
DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:5432/app_db

# Injected automatically by Render; defaults to 3000 locally.
# PORT=3000
# HOSTNAME=0.0.0.0
